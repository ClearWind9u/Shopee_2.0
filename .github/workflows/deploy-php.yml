name: 🚀 Deploy PHP to InfinityFree via FTP

on:
  push:
    branches:
      - main  # Chạy khi push lên nhánh main

jobs:
  ftp-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout source code
        uses: actions/checkout@v3

      - name: 📤 Deploy to FTP server
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ftpupload.net  # Thay bằng FTP host nếu khác
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          server-dir: /htdocs/  # Đường dẫn thư mục chứa PHP trên host
          local-dir: ./backend/  # Chỉ deploy thư mục backend
